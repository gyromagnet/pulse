repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.11.5
    hooks:
      # Run the linter.
      - id: ruff
        args: [./public/py]
      # Run the formatter.
      - id: ruff-format
        args: [./public/py]
